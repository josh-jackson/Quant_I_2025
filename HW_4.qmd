---
title: "hw4"
format: html
---

Instructions

Please complete this assignment using a quarto or RMarkdown file. Be sure to include your name in the YAML/preamble. When you turn in the assignment, include both the .qmd or .Rmd and knitted .html files. Please turn in both documents to the ASSIGNMENTS page on Canvas.

Put all R code in code chunks. They will behave as normal R code. If you are asked to explain something or write a few sentences, please do that outside of the code chunks. That way, when you knit, your sentences will show up as normal text, but all of your code will be in a designated code chunk.

You are allowed to Google or use your preferred AI-overlord. However, if you use code from an outside source (meaning not the lecture slides or the R help documentation), then you must provide a working link to the website you used. You don't need to use APA formatting or anything. Just a working url. You may discuss homework assignments with your classmates; however, it is important that you complete each assignment on your own and do not simply copy someone else’s code. If we believe one student has copied another’s work, both students will receive a 0 on the homework assignment. Please also list at the top of the document who you worked with.

Data: We will use the same data that simulation setup that we used in class, but tweak the number slightly. 


```{r, message = FALSE}
library(ggplot2)
library(dplyr)

# --- 1. Set Simulation Parameters ---

set.seed(2) # Makes your simulation reproducible

n_per_group <- 50  # 50 participants per group
residual_se <- 2 # This is the within-group SD

# Define the true means for our three groups.
# We'll set them apart to ensure the ANOVA finds a significant effect.

mean_group_A <- 2.0
mean_group_B <- 2.5
mean_group_C <- 3.0

# --- 2. Generate the Data ---

# Create the data for each group from a normal distribution

group_A_data <- rnorm(n_per_group, mean = mean_group_A, sd = residual_se)
group_B_data <- rnorm(n_per_group, mean = mean_group_B, sd = residual_se)
group_C_data <- rnorm(n_per_group, mean = mean_group_C, sd = residual_se)

# Combine into a single "long" data frame, which is required for ANOVA
simulated_data <- data.frame(
  dv = c(group_A_data, group_B_data, group_C_data),
  group = factor(rep(c("Group A", "Group B", "Group C"), each = n_per_group))
)

```


1. Run a one way anova using the above code. Then rerun the model simulating the same data but using the set.seed with 4
and 2. a. What is different among the models? b. Why did this difference occur? c. What could you do to minimize this difference? 

2. Using the original set.seed of 42, rerun the simulation this time changing the sigma/RSE to 1. a. How does these results differ? b. In the original (RSE = 2) what is the effect size difference between group A and B? c. In the smaller RSE model, what is the effect size? Why do they differ? 

3. Using the simulated data with set.seed = 2, create the ANOVA table. a. Explain how MSgroup is calculated b. What hypothesis is being tested with the F test? c. What number in the simulation is the MSR related to?   

4. Run a model with effect coding rather than default dummy coding. a. Interpret each regression coefficient. b. how does the ANOVA table differ between the dummy and effect coded models? 

5. Using either the dummy or the effect coded model: a. create predicted scores for each of groups. b. what are the CIs around them? Given that you simulated these data and know the means do you think they are reasonable?  c. Are the standard errors of each predicted mean the same or should they be different values because the SDs of each group differ?

6. Calculate the differences between each of the groups. Based on what you know given you simulated the data, could anything be considered lucky or unlucky?

7. What happens if we only sampled 30 per group rather than 50? 


